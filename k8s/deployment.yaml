apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: demo-app
  template:
    metadata:
      labels:
        app: demo-app
    spec:
      containers:
      - name: demo
        image: ghcr.io/mawirrr/linode-lke-demo:latest
        ports:
        - containerPort: 3000
        env:
        - name: VERSION
          value: IMAGE_TAG

